---
title: 7.3 Agent 生命周期 · Web3 Capital Network
chapter: 7
order: 3
description: WCN 里的 Agent 不是“永久外挂”。它们必须像节点一样，经历注册、授权、运行、复核、升级、暂停和退出，才能形成可信执行层。
---

<Hero badge="7.3 · Agent 生命周期" lead="WCN 里的 Agent 不是“永久外挂”。它们必须像节点一样，经历注册、授权、运行、复核、升级、暂停和退出，才能形成可信执行层。">
# Agent 不是随便接进来就一直运行，它也必须有生命周期。
</Hero>

<MetaGrid>
  <Meta label="本页作用" value="定义 Agent 状态机" />
  <Meta label="核心主题" value="Agent 也必须被治理" />
  <Meta label="阅读重点" value="注册、权限、运行、暂停、淘汰" />
</MetaGrid>

## Agent 的关键状态

## 生命周期路径

<Callout type="warning">
注册 → 授权 → 上线 → 日志记录 → 审核 / 复核 → 升级 / 暂停 / 淘汰
</Callout>

Agent 生命周期的设计目标，不是增加复杂度，而是保证：

## 为什么这一页重要？

因为如果 Agent 没有生命周期，系统会出现两个问题：
