-- Baseline migration: schema already applied via prisma db push
-- This migration captures the state delta from wave_a_audit to the current schema.
-- All changes (Phase 3-5, auth extensions, ecosystem expansion) are already in the database.
-- This file exists only to align the migration history.

SELECT 1;
