.DS_Store
node_modules
.next
.vercel
.claude/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env.*
/lib/generated/prisma
*.tsbuildinfo
coverage/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
.env*.local

# Office editor temp and lock files, often produced while reviewing WCN docs.
**/.~lock.*#
**/~$*
**/lu*.tmp

# Translation script caches (per source→target pair) — derived from content/wiki/
scripts/.translate-wiki-cache*.json

# Internal business binder (KYC/AML, co-investment, brand, soft-launch playbooks).
# NEVER commit — these are sensitive and must not be pushed to a public remote.
/WCN/
