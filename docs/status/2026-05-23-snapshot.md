# WCN Status Snapshot — 2026-05-23

- **Author**: Tech Lead (recorded by agent)
- **Branch surveyed**: `main` @ commit `217f6a0` (in sync with `origin/main`)
- **Build status**: `npm run build` passes; `vitest run` shows 111 files / 1205 tests green (per terminal `479729`)
- **Companion docs**: [`docs/delivery/q2-systematic-fix-roadmap.md`](../delivery/q2-systematic-fix-roadmap.md), [`docs/architecture/adr/0005-api-platform-layer.md`](../architecture/adr/0005-api-platform-layer.md)

## TL;DR

1. The working tree had a **26-file regression** that reverted multiple already-merged PRs (API platform layer, CI ratchets, Apply Sovereign content, prisma SSL helper, …). It has been discarded with `git restore .`. The repo is now clean against `main`.
2. The `/dashboard/projects` SSR error reported in production **cannot be confirmed from code alone** — the full SSR path compiles and types check, all selected Prisma fields exist in `schema.prisma`, and `npm run build` succeeds. A runtime trace (Vercel digest + function log) is required to close it out. Likely candidates are enumerated below.
3. While reading the SSR path, **one real correctness bug was found**: `app/[locale]/dashboard/projects/page.tsx` calls `redactProjectForMember(p, userId)` but the Prisma `select` does **not** include `node.ownerUserId`, so the ownership check always evaluates `false` and every NODE_OWNER sees their own projects with contact / fundraising / pitch URL nulled out. Not a crash, but a visible UX regression for non-admin users. Targeted fix below.
4. Tooling debt that blocks day-to-day work: `npm run lint` is broken under ESLint 9 / flat config (`Invalid Options` for `useEslintrc` etc.); `package.json` already has the `lint` script but the configuration doesn't load cleanly. Listed in pending tasks.

---

## 1. Audit of uncommitted working tree (before restore)

Initial `git status` showed 26 modified files plus the untracked `WCN/` document binder. Every modification was directional: **delete recently-merged work, write back the older version**. Files and PRs that would have been erased:

| File | Reverted PR / Commit |
|---|---|
| `app/api/applications/route.ts` | `217f6a0` — typed `route.public()` (ADR-0005 first migration) |
| `lib/permissions.ts` + `lib/__tests__/permissions.test.ts` | NODE_OWNER `pob: create` restoration |
| `lib/prisma.ts` | `shouldUsePrismaSsl()` helper |
| `lib/rate-limit.ts` | `RATE_LIMIT_DISABLED` / Upstash-missing override (used by e2e CI) |
| `middleware.ts` | matcher regex + `/api/applications` public path simplification |
| `next.config.mjs` | `poweredByHeader: false` hardening |
| `package.json` | `lint:fix` / `typecheck` / `check` / `check:i18n*` / `check:metrics*` scripts + 3 ESLint deps |
| `messages/*.json` × 10 | ~55 keys/locale × 10 locales (Apply Sovereign, about formal definition, archLayer, success states) — `c39b01e` |
| `app/globals.css` | −1290 lines (Apply Sovereign block) — `c39b01e` |
| `lib/docs.ts` | docs search-index + adjacency cache |
| `scripts/e2e-business-loop.ts` | `leadOwnerId`/`capitalOwnerId` two-owner refactor |

**Diagnosis**: not deliberate work — characteristic of an IDE / merge / folder-copy accident that brought older file snapshots back to disk. No commit was created; `git restore .` reset to HEAD. `WCN/` document binder left in place as before.

---

## 2. `/dashboard/projects` runtime error — investigation plan

### What the code review confirms

- Server entry `app/[locale]/dashboard/projects/page.tsx`:
  - `getServerSession`, `getOwnedNodeIds`, `memberProjectsWhere`, two `prisma.findMany` (`Project` + `Node`), `redactProjectForMember` per row, then `<ProjectsConsole>` client component.
  - All selected fields exist in `schema.prisma` (`Project.{id,name,sector,stage,status,internalScore,createdAt,nodeId}`, `Node.{id,name,type}`).
  - No raw SQL, no top-level `await` on a third-party service, no on-render `fetch`.
- Client `app/[locale]/dashboard/projects/ui.tsx`:
  - Uses `recharts` `ResponsiveContainer` — already wrapped in `"use client"`, will not break SSR.
  - All shape-required props of `ProjectRow` are sent from the server.
- Detail entry `app/[locale]/dashboard/projects/[id]/page.tsx`: shape matches `ProjectData` in `ui.tsx` exactly.

The build (`npm run build`) compiles every route under `/dashboard/projects/*`, so this is not a compile-time / type / import error.

### Most likely runtime causes (in order)

| # | Hypothesis | Why it fits | Verification |
|---|---|---|---|
| 1 | **Postgres connection refused under `getPrisma()`** in prod (SSL mode or pool exhaustion) | `lib/prisma.ts` defaults `max=2` per instance; `shouldUsePrismaSsl()` enables SSL in prod unless URL has `sslmode=disable`. A misset `DB_SSL` / `?sslmode=` would throw at first query. | Check Vercel function log on `/zh/dashboard/projects` for `pg`/`PrismaClient` errors; or fetch `/api/health`. |
| 2 | **Prisma schema drift vs prod DB** — e.g. a column referenced in `select` doesn't exist in deployed DB | Last migration was `20260430120000_composite_indexes_for_list_queries`; the projects page reads `internalScore`. If the prod DB was provisioned before `20260408175227_phase1_mvp` ran cleanly, this column may be missing. | `vercel env pull && npx prisma migrate status` against prod DB URL. |
| 3 | **`getServerSession(authOptions)` returns malformed session** under specific cookie state | The page hard-redirects on missing user, but if `session.user.role` is undefined it propagates into `isAdminRole(role)` → returns `false` (safe) and `getOwnedNodeIds(prisma, undefined)` → Prisma throws "Argument userId must not be undefined" | Re-test signed in vs signed out with a fresh cookie; capture the digest. |
| 4 | **`JSON.parse(JSON.stringify(...))` chokes on a `Decimal` field** | None of the selected fields are `Decimal` (only `Float?`), so unlikely. | n/a |

### What I cannot do from here

Without access to Vercel function logs or the SSR digest from a failed request, hypothesis 1 vs 2 vs 3 cannot be disambiguated. Production runtime errors require runtime evidence.

### Recommended next step on the user side

Run **one** of the following against production, then paste the output back:

```bash
# Option A — get the digest from a failed request
vercel logs --since 30m | rg -i 'digest|projects|prisma|pg'

# Option B — confirm DB schema state
vercel env pull .env.production.local
DATABASE_URL=$(grep ^POSTGRES_URL .env.production.local | cut -d= -f2-) \
  npx prisma migrate status
```

Once we have either the digest or the migration status, the fix is ~one PR.

---

## 3. Real bug found while reading the SSR path

**File**: `app/[locale]/dashboard/projects/page.tsx`
**Severity**: correctness regression for NODE_OWNER role (not a crash)

```31:45:app/[locale]/dashboard/projects/page.tsx
prisma.project.findMany({
  where: projectWhere,
  orderBy: { createdAt: "desc" },
  take: 200,
  select: {
    id: true,
    name: true,
    sector: true,
    stage: true,
    status: true,
    internalScore: true,
    createdAt: true,
    nodeId: true,
    node: { select: { id: true, name: true } },
  },
}),
```

`redactProjectForMember` then evaluates `project.node?.ownerUserId === viewerUserId`:

```32:43:lib/member-redact.ts
export function redactProjectForMember<T extends Record<string, any>>(
  project: T,
  viewerUserId: string
): T {
  const isOwner = project.node?.ownerUserId === viewerUserId;
  if (isOwner) return project;

  const cleaned: any = { ...project };
  for (const key of PROJECT_PII_KEYS) cleaned[key] = null;
  if (cleaned.node) cleaned.node = redactNodeForMember(cleaned.node);
  return cleaned;
}
```

Because `ownerUserId` is not in the `select`, `isOwner` is always `false`. Every NODE_OWNER user sees their own projects with `contactName / contactEmail / contactTelegram / fundraisingNeed / pitchUrl` nulled out. The detail page `[id]/page.tsx` selects `ownerUserId` correctly — only the list page is affected.

**Fix** (one-line, scoped):

```diff
- node: { select: { id: true, name: true } },
+ node: { select: { id: true, name: true, ownerUserId: true } },
```

Plus a `redactNodeForMember` call afterwards on the list view (already happens inside `redactProjectForMember`'s `cleaned.node` branch — so the leak is also auto-closed when the bug is fixed, since the owner branch returns the full project).

No tests cover this matrix today; recommended companion test: `lib/__tests__/member-redact.test.ts` adding a case where `viewerUserId === project.node.ownerUserId` returns the un-redacted row.

---

## 4. Engineering / tooling debt blocking flow

| # | Item | Impact | Effort |
|---|---|---|---|
| T1 | `npm run lint` fails: ESLint 9 / flat-config doesn't load (`Invalid Options: useEslintrc, extensions, ignorePath, rulePaths, reportUnusedDisableDirectives`) | CI ratchet on lint gates is essentially a no-op; new code can't be lint-gated locally | 0.5 day — migrate `.eslintrc*` → `eslint.config.mjs` flat config |
| T2 | `POST /api/applications` is public and has no IP rate-limit (middleware skips rate-limit on public paths) | Spam / DoS surface on the marketing funnel | 0.5 day — explicit `rateLimit('write')` in the route + IP key |
| T3 | Two coexisting i18n approaches (`useAutoTranslate` + `useTranslations`) | Onboarding friction; bundle includes both runtime translators | 1 day — pick one, migrate the smaller set |
| T4 | `relativeTime()` duplicated in ~6 dashboard `ui.tsx` files | Drift risk | 0.25 day — `lib/format.ts` |
| T5 | Admin-panel header pattern (`adminSaving` + `adminMsg`, amber left-border card, "Saved/Saving…" pill) repeated in 5+ files | Inconsistent feedback, painful to restyle | 0.5 day — `AdminPanelCard` + `useAdminPatch` hook |

These are not P0; they're "every new PR adds a little to the bill" debt that should be paid off cohort-by-cohort per the [Q2 roadmap](../delivery/q2-systematic-fix-roadmap.md).

---

## 5. Recent merged work (last 2 weeks, in order)

| Commit | Title | Why it matters |
|---|---|---|
| `217f6a0` | feat(core): API platform layer (#41) | ADR-0005 — typed `route.permission/public/admin` builder. Migration cookbook in `lib/core/api/README.md`. First route migrated: `app/api/applications/route.ts`. |
| `af6ec55` | feat(ci): metrics ratchet gate (#40) | `scripts/metrics-gate.ts` blocks regressions on code-shape metrics (`: any`, `force-dynamic`, `JSON.parse(JSON.stringify)` count). |
| `16b0e1b` | feat(ci): i18n parity ratchet (#39) | `scripts/check-i18n.ts` blocks locale drift from `en.json`. |
| `724478a` | fix(ci): restore P0 engineering gates (#42) | Re-armed tsc/test/lint gates in `ci.yml`. |
| `dea6bc2` | docs: ADR-0005 + Q2 roadmap + Q1 evaluation report (#37) | Sets the Q2 plan that every subsequent PR aligns to. |
| `00895a0` | fix(ci): unblock e2e suite — 4 layered fixes (#28) | E2E now runs against `next start` + service-container Postgres + no Redis. |
| `c39b01e` | feat(apply): full Sovereign alignment | The Apply page redesign that the regression above tried to undo. |
| `63eaede` | fix(home): universal stress-test fixes | Home page tightening. |
| `965fed8` | fix(about): honesty pass | About page claims trimmed back to verifiable refs. |

---

## 6. Concrete next steps (ordered, no work started)

1. **Confirm the projects-page error** with one of the two prod commands in §2 above; share the digest or migration status. (User action)
2. **Apply the redaction fix** in §3 — one-line `select` + a regression test. (~30 min)
3. **Migrate ESLint flat config** so `npm run lint` runs again. (T1; half-day)
4. **Add explicit IP rate-limit to `POST /api/applications`** via the new `route.public(...)` typed builder, reusing the `write` profile. (T2; half-day, also serves as ADR-0005 cohort progress)
5. **Schedule the duplication cleanup** (T3–T5) into a single Week-3 cohort PR per the Q2 roadmap.

Everything else (mobile / PWA strategy, Q2 cohort planning beyond Week 1) is tracked in `docs/delivery/q2-systematic-fix-roadmap.md` and does not need a separate entry here.
